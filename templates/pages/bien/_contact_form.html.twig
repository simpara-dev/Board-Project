{{form_start(form)}}
<div class="row">
	<div class="col">{{form_row(form.nom)}}</div>
	<div class="col">{{form_row(form.prenom)}}</div>
</div>
<div class="row">
	<div class="col">{{form_row(form.numeroTelephone)}}</div>
	<div class="col">{{form_row(form.email)}}</div>
</div>
{{form_rest(form)}}
<input type="submit" class="btn btn-lg btn-success" value="{{valeurBtnSubmit|default("Envoyer")}}" formnovalidate>
{{form_end(form)}}
